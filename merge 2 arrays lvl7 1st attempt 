const mergeArrays = (a,b) => {
 let array = [];
  
let resultArr = a.map((n,index) => {
console.log(n + b[index])
  
})

}


// find the index of the first array
// 

mergeArrays([1, 2, 3, 4, 5, 6, 7, 8], ['a', 'b', 'c', 'd', 'e'])
  //[1, "a", 2, "b", 3, "c", 4, "d", 5, "e", 6, 7, 8]);