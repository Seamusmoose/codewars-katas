function removeSmallest(numbers) {
let copy = [...numbers]
let smallest = Math.min(...numbers);
let smallestIdx = numbers.indexOf(smallest)
copy.splice(smallestIdx, 1)
return copy
}

removeSmallest([3, 2, 1, 4, 5])
  //[2, 3, 4, 5], "Wrong result for [1, 2, 3, 4, 5]");


